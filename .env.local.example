# Supabase Configuration (Optional)
#
# Collaborative features (shared annotation projects) require Supabase.
# Without these variables, the app runs in local-only mode.
#
# To enable collaboration:
# 1. Create a free project at https://supabase.com
# 2. Go to Settings > API to find your credentials
# 3. Copy this file to .env.local and fill in your values

# Your Supabase project URL (from Settings > API)
NEXT_PUBLIC_SUPABASE_URL=

# Your Supabase anon/public key (from Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# ----------------------------------------
# Supabase Setup Instructions
# ----------------------------------------
#
# After creating your Supabase project, run this SQL in the SQL Editor
# to create the required tables:
#
# -- See knowledge/CCS-WB-memory/WORKING.md for the full schema
#
# Then configure authentication providers:
# 1. Go to Authentication > Providers
# 2. Enable Google: Add your Google OAuth credentials
# 3. Enable GitHub: Add your GitHub OAuth app credentials
#
# Note: Magic link (email) auth works out of the box, but you may want
# to configure a custom SMTP server in Authentication > Email Templates
